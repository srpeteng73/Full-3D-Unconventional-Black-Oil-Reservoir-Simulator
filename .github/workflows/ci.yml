name: CI (paused)

on:
  workflow_dispatch:  # Only runs when you click "Run workflow" manually

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: CI paused
        run: echo "CI is intentionally paused. No tests run."
